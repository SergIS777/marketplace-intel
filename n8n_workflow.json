{
  "name": "LOWENGRASS Intel — утренний отчёт",
  "nodes": [
    {
      "parameters": { "rule": { "interval": [ { "field": "hours", "hoursInterval": 24, "triggerAtHour": 9 } ] } },
      "id": "a1", "name": "Cron 09:00",
      "type": "n8n-nodes-base.scheduleTrigger", "typeVersion": 1.2,
      "position": [200, 300]
    },
    {
      "parameters": { "command": "echo '===ALERTS==='; cat /files/mi-data/events_latest.json 2>/dev/null; echo; echo '===CARDS==='; cat /files/mi-data/wb_cards_latest.csv 2>/dev/null; echo; echo '===DRAFTS==='; head -6 /files/mi-data/drafts_latest.csv 2>/dev/null; echo" },
      "id": "a2", "name": "Read Data",
      "type": "n8n-nodes-base.executeCommand", "typeVersion": 1,
      "position": [420, 300]
    },
    {
      "parameters": { "jsCode": "const out = $input.first().json.stdout || ''; const parts = {}; let cur = null; out.split('\\n').forEach(line => { if (line.indexOf('===') === 0) { cur = line.replace(/=/g, ''); parts[cur] = []; return; } if (cur && line.trim()) parts[cur].push(line); }); const icon = {CRITICAL:'🚨', WARNING:'⚠️', INFO:'ℹ️'}; let m1 = '🚨 Алерты LOWENGRASS:\\n\\n'; try { JSON.parse((parts['ALERTS']||[]).join('\\n')||'[]').forEach(e => { m1 += (icon[e.type]||'•')+' '+e.message+'\\n'; }); } catch(e) { m1 += 'нет данных\\n'; } let m2 = '📦 Остатки:\\n\\n'; (parts['CARDS']||[]).slice(1).forEach(l => { const c = l.split(','); const s = parseInt(c[9]); const flag = s<=3?'🚨':(s<=10?'⚠️':'✅'); m2 += flag+' '+c[2].slice(0,30)+' — '+s+' шт, '+Math.round(parseFloat(c[7]))+'₽\\n'; }); const dn = Math.max(0,(parts['DRAFTS']||[]).length-1); const m3 = '✍️ Черновики ответов на отзывы: '+dn+'\\nОткройте бота → «Черновики» → Одобрить/Отклонить.'; return [{json:{text:m1}},{json:{text:m2}},{json:{text:m3}}];" },
      "id": "a3", "name": "Format 3 Messages",
      "type": "n8n-nodes-base.code", "typeVersion": 2,
      "position": [640, 300]
    },
    {
      "parameters": { "chatId": "ВСТАВЬ_СВОЙ_CHAT_ID", "text": "={{ $json.text }}", "additionalFields": {} },
      "id": "a4", "name": "Telegram Send",
      "type": "n8n-nodes-base.telegram", "typeVersion": 1.2,
      "position": [860, 300]
    }
  ],
  "connections": {
    "Cron 09:00": { "main": [[{ "node": "Read Data", "type": "main", "index": 0 }]] },
    "Read Data": { "main": [[{ "node": "Format 3 Messages", "type": "main", "index": 0 }]] },
    "Format 3 Messages": { "main": [[{ "node": "Telegram Send", "type": "main", "index": 0 }]] }
  }
}
